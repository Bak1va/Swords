name: Deploy Backend

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            cd /var/www/poker/backend/Swords
            git pull origin main
            npm ci
            npm run build
            /home/github-deploy/node_modules/.bin/pm2 delete poker-backend || true
            /home/github-deploy/node_modules/.bin/pm2 start dist/server.js --name poker-backend
            /home/github-deploy/node_modules/.bin/pm2 save